{
  "name": "theme",
  "version": "1.0.0",
  "description": "Compilation step for the site files.",
  "private": true,
  "scripts": {
    "build:stylesheet": "sass --style=compressed scss/styles.scss:../site/styles.css",
    "build": "run-s build:stylesheet"
  },
  "devDependencies": {
    "sass": "^1.56.1",
    "npm-run-all": "^4.1.5"
  }
}
